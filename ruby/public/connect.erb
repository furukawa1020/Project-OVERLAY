<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OVERLAY Connection</title>
    <style>
        body { 
            background: #000; 
            color: #fff; 
            font-family: sans-serif; 
            display: flex; 
            flex-direction: column;
            align-items: center; 
            justify-content: center; 
            height: 100vh; 
            margin: 0; 
        }
        h1 { font-size: 2rem; margin-bottom: 2rem; }
        .qr-container { 
            background: white; 
            padding: 20px; 
            border-radius: 10px; 
        }
        p { margin-top: 2rem; font-size: 1.2rem; color: #888; }
        .url { color: #4CAF50; font-weight: bold; }
    </style>
</head>
<body>
    <h1>Join Session</h1>
    
    <div class="qr-container">
        <%= qr_svg %>
    </div>

    <p>Scan to join or visit:</p>
    <p class="url"><%= url %></p>
</body>
</html>
